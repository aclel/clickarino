version: 0.2
            
phases:
  install:
    commands:
      - cd lambda && npm install -g serverless
      - npm install
  build:
    commands:
      - serverless package
  post_build:
    commands:
      - serverless deploy --package=.serverless/clickarino.zip
